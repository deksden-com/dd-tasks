# dd-flow creates feature worktrees before PLAN.  Secret-bearing local env
# files are copied only through .worktreeinclude; no dependency install or
# application process is started during the planning handoff.
