---
file: '.memory-bank/protocol/PRT-004-exe-preview-runtime/trace/20260804T203419+0200-protocol-start.md'
description: 'Стартовый trace protocol flow для Exe preview runtime.'
purpose: 'Фиксирует preflight, route, base и границу stable-root handoff.'
version: '0.1.0'
date: '2026-08-04'
status: 'ACTIVE'
c4_level: 'operations'
parent: '.memory-bank/protocol/PRT-004-exe-preview-runtime/index.md'
tags: [trace, protocol, specify, exe-dev]
---

# Prompt start: protocol.md

- timestamp: `2026-08-04T20:34:19+02:00`
- prompt: `protocol.md`
- protocol: `PRT-004-exe-preview-runtime`
- cwd: stable root `/Users/deksden/Documents/_Projects/dd-tasks`
- branch_or_worktree: `feature/prt-004-exe-preview-runtime` at the declared service checkout
- trigger: user approved protocolization, operational runbook/stage policy and later Luna implementation handoff
- understood_objective: prepare a reproducible, safe Exe.dev preview contour for an exact dd-tasks checkpoint
- current_stage: `specify`
- expected_next_action: continue optimized SPECIFY from the feature worktree
- route: `feature_worktree`; source delivery by merge; provider mutation by later `deploy.md`
- scope_boundaries: runtime/policy/runbook/scenario only; no provider mutation, production or background machinery
- assumptions: private disposable preview with deterministic fixtures is the proposed safe default
- blockers: expected Exe.dev ownership context remains a potential user question

## Preflight

- CLI `0.4.0`: compatibility `ok`; Memory Bank/flow-pack drift `same`.
- Git: stable `main` clean at `4c26c33e100da970ac54b5560b6dfbb0e8d9e0cc`; no pre-existing worktree.
- CLI permissions command returned no assessable JSON/output; manual readable/writable checks and temporary probes in `.memory-bank` and `.tasks` passed and probes were removed.
- Worktrunk is unavailable. The correctly scoped Git worktree was created by
  explicit `git worktree add`; `dd-flow worktree create` returned structured
  `tool_unavailable`, while protocol/runtime state remains registered against
  the exact workspace path.
- Active DEF: `DEF-MBU-RUNTIME-ACTIVE-STATE` classified `not_relevant` because this protocol does not migrate dd-flow runtime/home primary data.

The current Codex session is not rooted in the new worktree. Per the project
flow contract it creates only this minimal protocol handoff; substantive
SPECIFY/PLAN and project tooling belong to a session rooted at the feature
worktree.
