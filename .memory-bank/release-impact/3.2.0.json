{
  "schema_id": "dd-flow/release-impact@1",
  "from_version": "3.1.0",
  "to_version": "3.2.0",
  "minimum_upgrade_mode": "targeted_minor",
  "domains": [
    "canonical_layer",
    "engineering",
    "operations",
    "plans",
    "protocol_evidence"
  ],
  "path_migration": false,
  "runtime_migration": true,
  "required_checks": [
    "mb-lint",
    "git diff --check",
    "target-driven engine routing and storage safety eval",
    "engine schema registry identity and checksum validation",
    "RUN engine binding schema validation",
    "plan item execution guidance eval",
    "release-impact schema validation",
    "compatibility schema and consistency validation",
    "linked CLI 0.7.0 registry/tag/artifact-commit reconciliation"
  ],
  "notes": "This minor release adds target-driven engine routing, explicit storage migration safety, immutable RUN engine bindings, engine schema registry validation and self-contained plan execution guidance. Existing canonical paths remain valid and legacy RUN recovery is retained; use targeted minor review and the coordinated dd-flow-cli 0.7.0 release."
}
